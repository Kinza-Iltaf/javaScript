"use strict" // now the js code will be treat as the newer js code like latest js version code.
// varialble declaration
let name = " Adam "
let age = 23
let isLoggedIn = true

console.table([name,age, isLoggedIn])

// some data types
// number
// bigint
// string = ""
// boolean = true/false
// null => standalone value
// undefined => not define a value means variable is declared but does not assigned any value to it.
// symbol => when we are talking about unique value
// object
// null => its type of object (interview question)
// undefined => its type is undefined
console.log(age, typeof(age))